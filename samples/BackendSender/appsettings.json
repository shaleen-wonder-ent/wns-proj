{
  // Copy this file to appsettings.local.json and fill in real values there.
  // appsettings.local.json is gitignored so secrets are never committed.
  "NotificationHub": {
    "HubName": "<your hub name e.g. wns-demo-hub>",
    "FullConnectionString": "<DefaultFullSharedAccessSignature connection string>"
  }
}
